# Lower bounds only, so the resolver picks recent versions that ship
# prebuilt wheels for the Streamlit Cloud Python (currently 3.14) —
# avoids slow/failing source builds of numpy/pandas.
streamlit>=1.40
plotly>=5.24
numpy>=2.3
pandas>=2.3
